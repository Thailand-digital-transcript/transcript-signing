package com.wpanther.transcript.signing.domain.model;

/** PENDING: not yet signed. SIGNED: embedded + uploaded (terminal success). FAILED: embed failed. */
public enum BatchItemStatus { PENDING, SIGNED, FAILED }
